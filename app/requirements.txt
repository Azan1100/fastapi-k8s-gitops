# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ── Redis ─────────────────────────────────────────────────────────────────────
# redis[hiredis] — hiredis is an optional C-extension parser (~10× faster)
redis[hiredis]>=5.2.0

# ── OpenTelemetry SDK ─────────────────────────────────────────────────────────
opentelemetry-api>=1.29.0
opentelemetry-sdk>=1.29.0

# ── OTLP Exporters (gRPC) ────────────────────────────────────────────────────
# Sends spans + metrics + logs to SigNoz collector on port 4317
opentelemetry-exporter-otlp-proto-grpc>=1.29.0

# ── Auto-Instrumentation ─────────────────────────────────────────────────────
# Automatically wraps FastAPI request handling with spans
opentelemetry-instrumentation-fastapi>=0.50b0

# Automatically wraps every Redis command with a child span
opentelemetry-instrumentation-redis>=0.50b0

# Injects trace_id/span_id into Python log records
opentelemetry-instrumentation-logging>=0.50b0

# ── HTTP Client (used in Dockerfile healthcheck) ──────────────────────────────
httpx>=0.27.0
